import {Action} from '../action/action.model';

export class AssessmentRating{
    assessmentRatingId: string;
    assessmentRatingDescrption: string;
    assessmentRating: string;
    assessmentRatingComment: string;
    actions: Array<Action>; 
}

                   