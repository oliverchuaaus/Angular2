Create component that:
1. has 3 links: home, detail and about
2. each link will show a different component
3. home is shown by default
4. Use HashLocationStrategy
5. Make detail a parameterized route, and show id in component

Extend application to:
1. Add login component and restricted component.
2. Restricted component should only be accessed if logged in.

Extend application to:
1. Add Product component and nested components: Main, Search, Maintain, Detail (parameterized route)

This project shows how to:
1. Use routing
2. Use HashLocationStrategy to make it work in webapps
3. Use parameterized routes
4. Use nested routes